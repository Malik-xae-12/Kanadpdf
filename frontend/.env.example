# Frontend environment variables (Vite exposes these as import.meta.env.VITE_*)
# In development the Vite proxy handles /api, so you only need the API key.
# In production, set the base URL to your backend.

VITE_API_BASE_URL=/api
VITE_API_KEY=changeme-in-production
